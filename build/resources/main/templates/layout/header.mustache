<!DOCTYPE HTML>
<html>
<head>
    <title>스프링부트 웹서비스</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
<!-- css는 헤더에 있고, js는 footer에 위치하는 이유 : 페이지 로딩 속도롤 높이기 위함.

HTML은 위에서 부터 코드가 실행되기 때문에 header가 다 실행된후 body가 실행된다. header가 다 불러지지 않으면 사용자 쪽에선 백지 화면 노출됨.
js 용량이 클수록 body 부분 실행이 느려지기때문에 js는 body하단에 두어 화면이 다 그려진후 호출하는 것이 좋음

-->